import React, { Component } from 'react';
import { withRouter, Link } from 'react-router-dom';

class GridSearch extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <div>网格化搜捕</div>;
  }
}

export default withRouter(GridSearch);
